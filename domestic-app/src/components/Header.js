// Header.js
import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>DomesticApp</h1>
      {/* Agrega más elementos según sea necesario, como botones de inicio de sesión, etc. */}
    </header>
  );
};

export default Header;
